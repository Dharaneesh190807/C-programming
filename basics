#include <stdio.h>
void main() {
    int a,b,sum;
    printf("Enter any two numbers:");
    scanf("%d%d",&a,&b);
    sum=a+b;
    printf("sum of the numbers:%d",sum);
    
}
Modification tasks:
1.Find difference

#include <stdio.h>
void main() {
    int a,b,difference;
    printf("Enter any two numbers:");
    scanf("%d%d",&a,&b);
    difference=a-b;
    printf("difference of the numbers:%d",difference);
    
}
2.Find product

#include <stdio.h>
void main() {
    int a,b,c;
    printf("Enter any two numbers:");
    scanf("%d%d",&a,&b);
    c=a*b;
    printf("product of the numbers:%d",c);
    
}
3.Find quotient

#include <stdio.h>
void main() {
    int a,b,c;
    printf("Enter any two numbers:");
    scanf("%d%d",&a,&b);
    c=a/b;
    printf("quotient of the numbers:%d",c);
    
}
4. using three numbers

#include <stdio.h>
void main() {
    int a,b,c,sum;
    printf("Enter any three numbers:");
    scanf("%d%d%d",&a,&b,&c);
    sum=a+b+c;
    printf("sum of the numbers:%d",sum);
    
}
5.Find average

#include <stdio.h>
void main() {
    int a,b,c,avg;
    printf("Enter any three numbers:");
    scanf("%d%d%d",&a,&b,&c);
    avg=(a+b+c)/3;
   
    printf("average of the numbers:%d",avg);
    
}
